# GIT: source control, tracks files and folders
# conpare the prev state and current state of the folder
# Initialize local git
git init

# Add file to the git
git add file_name   //add single files
git add .           //add multiple files

# Commit file to git
git commit =m "message"

# Status check of this repo
git status

# Check logs
git log

# Global user and email/ defining identity
git config --global user.name "your_name"
git config --global ser.email your_email

# Check branch
git branch

# Create branch
git branch branch_name

# Switch Branches
git switch branch_name
git checkout branch_name

### If euta branch ma maile files banayo ki arko branch ma dekhaudaina merge nagare samma.


# Merge Branches
git merge branch_name

# show which repo are we in github
git remote -v